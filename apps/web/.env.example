# Optional: Cesium Ion access token for Cesium World Imagery (road map with labels).
# Create a free token at https://ion.cesium.com/tokens
# When unset, the app uses CARTO Voyager tiles (no key required).
VITE_CESIUM_ION_TOKEN=
